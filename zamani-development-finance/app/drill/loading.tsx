import { DrillPageSkeleton } from "@/components/skeleton";

/** Covers every /drill/* route, including the [param] detail pages. */
export default function Loading() {
  return <DrillPageSkeleton />;
}
